/**
 * Path segments generated from page titles.
 *
 * The generator lives in `@clewwiki/content/slug`; see `./paths` for why. The
 * import pipeline calls the same functions, so a page created by an import and
 * a page created by the form land on the same path for the same title.
 */

export * from '@clewwiki/content/slug';
